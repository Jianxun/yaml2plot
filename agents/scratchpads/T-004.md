# T-004 Scratchpad

## Task summary (DoD + verify)
- Title: Add pandas DataFrame + CSV support to plotting data pipeline
- DoD:
  - Extend `plot()` data normalization to accept `pandas.DataFrame` inputs while preserving existing dict and xarray support.
  - Add CSV helper loader APIs in `src/yaml2plot/loader.py` and export them from package `__init__`.
  - Optionally support `.csv` path auto-detection in `plot()` path input flow with clear error handling.
  - Add/refresh unit tests for DataFrame input, CSV loading, and path routing behavior.
  - Update user docs for DataFrame/CSV usage and x-axis/index mapping expectations.
- Verify:
  - `pytest tests/unit/plotting -v`
  - `pytest tests/unit/loader -v`
  - `pytest tests/workflows -k "plot or yaml" -v`
  - `./venv/bin/python scripts/lint_tasks_state.py`

## Read (paths)
- `agents/context/lessons.md`
- `agents/context/contract.md`
- `agents/context/tasks.yaml`
- `agents/context/tasks_state.yaml`
- `agents/context/project_status.md`

## Plan
- Add/adjust tests for DataFrame and CSV support (TDD loops).
- Implement loader CSV APIs and exports.
- Extend plotting normalization/path routing for DataFrame and `.csv`.
- Update docs for new input modes and x/index behavior.
- Run verification, finalize notes, open PR.

## Milestone notes
- Intake complete.
- Added failing tests for loader CSV APIs, DataFrame input normalization, and `.csv` path routing.
- Implemented loader + plotting + exports to satisfy new tests.
- Added workflow coverage for plotting from CSV path and updated user docs.

## Patch summary
- Added `load_csv_data()` and `load_csv_data_batch()` in `src/yaml2plot/loader.py`.
- Extended `plot()` normalization in `src/yaml2plot/core/plotting.py` to accept pandas DataFrame and route `.csv` paths through CSV loader.
- Exported CSV helpers from `src/yaml2plot/__init__.py`.
- Added unit tests in `tests/unit/loader/test_loader_basic.py` and `tests/unit/plotting/test_plotting_helpers.py`.
- Added workflow test `tests/workflows/test_csv_plot.py`.
- Updated DataFrame/CSV usage docs in `README.md`, `docs/quickstart.rst`, and `docs/api.rst`.

## PR URL
- Pending.

## Verification
- `./venv/bin/python -m pytest tests/unit/plotting -v` (pass)
- `./venv/bin/python -m pytest tests/unit/loader -v` (pass)
- `./venv/bin/python -m pytest tests/workflows -k "plot or yaml" -v` (pass)
- `./venv/bin/python scripts/lint_tasks_state.py` (pass)

## Status request (Done / Blocked / In Progress)
- In Progress

## Blockers / Questions
- None.

## Next steps
- Push branch, open PR, update task state to `ready_for_review` with PR number.
