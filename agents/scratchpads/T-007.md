# T-007 Scratchpad

## Task summary (DoD + verify)
- DoD:
  - Ensure CLI and Python API behavior are consistent for DataFrame/CSV inputs.
  - Add negative-path tests (missing x, ambiguous index, bad CSV schema) with actionable errors.
  - Add migration notes from dict-centric examples to DataFrame-friendly patterns.
- Verify:
  - `pytest tests/unit/plotting -v`
  - `pytest tests/unit/loader -v`
  - `pytest tests/workflows -k "plot or yaml" -v`

## Read (paths)
- `agents/roles/executor.md`
- `agents/context/lessons.md`
- `agents/context/contract.md`
- `agents/context/tasks.yaml`
- `agents/context/tasks_state.yaml`
- `agents/context/project_status.md`

## Plan
- Inspect current DataFrame/CSV behavior in plotting, loader, and CLI.
- Add failing tests for parity and negative paths.
- Implement minimal fixes to satisfy tests and preserve current contracts.
- Update README/docs migration guidance.
- Run required verification commands and capture results.

## Milestone notes
- Intake complete; task selected and bootstrapped.
- Added failing tests for:
  - DataFrame missing `x.signal` actionable error text.
  - Ambiguous DataFrame `index` alias schema.
  - CSV empty/single-column schema rejection.
  - CLI path-based CSV routing parity with Python API.
- Implemented adapter and validation fixes; reran required test suites.
- Added migration guidance in README + quickstart docs.

## Patch summary
- `src/yaml2plot/cli.py`
  - Route `plot` input through `normalize_plot_data(final_raw_file)` for both SPICE and CSV paths.
  - Keep user-facing load message, now suffix-aware (`SPICE` vs `CSV`).
- `src/yaml2plot/core/plotting.py`
  - Added DataFrame schema guard for ambiguous unnamed-index + `index` column conflicts.
  - Improved missing X-axis signal error with DataFrame/CSV-specific guidance.
- `src/yaml2plot/loader.py`
  - Added CSV schema validation for empty/missing-header files and single-column files.
  - Raised actionable `ValueError` messages for these invalid CSV schemas.
- `tests/unit/plotting/test_plotting_helpers.py`
  - Added negative-path tests for missing `x.signal` guidance and ambiguous index handling.
- `tests/unit/loader/test_loader_basic.py`
  - Added negative-path tests for empty and single-column CSV schema.
- `tests/unit/cli/test_cli_raw_field.py`
  - Updated plot command tests to assert shared normalization path usage.
  - Added CSV raw-field parity test.
- `README.md`, `docs/quickstart.rst`
  - Added migration notes from dict-centric usage to DataFrame/CSV-friendly patterns.

## PR URL
- Pending.

## Verification
- `./venv/bin/python -m pytest tests/unit/plotting -v` (pass)
- `./venv/bin/python -m pytest tests/unit/loader -v` (pass)
- `./venv/bin/python -m pytest tests/workflows -k "plot or yaml" -v` (pass)

## Status request (Done / Blocked / In Progress)
- Done

## Blockers / Questions
- None.

## Next steps
- Open PR, set task state to `ready_for_review`, and hand off to Reviewer.
