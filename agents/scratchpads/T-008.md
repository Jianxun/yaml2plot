# T-008 Scratchpad

## Task summary (DoD + verify)
- Ensure `pyproject.toml` and package runtime version metadata match.
- Update README/docs examples to match current supported data contracts and API behavior.
- Remove stale tuple/dict-only narratives that conflict with implemented behavior.

Verify:
- `pytest -q`
- `make docs`

## Read (paths)
- agents/context/lessons.md
- agents/context/contract.md
- agents/context/tasks.yaml
- agents/context/tasks_state.yaml
- agents/context/project_status.md
- agents/roles/executor.md

## Plan
- [x] Identify version mismatches and stale docs narratives.
- [x] Add/adjust tests first when behavior or metadata expectations need lock-in.
- [x] Implement focused code/docs updates for T-008 scope.
- [x] Run required verification and record results.
- [ ] Prepare closeout and status handoff for review.

## Milestone notes
- Intake complete.
- Added `tests/unit/test_package_metadata.py` and confirmed it failed on runtime version mismatch (`2.0.0` vs `2.0.1`), then fixed `src/yaml2plot/__init__.py`.
- Updated README/docs version references to `2.0.1` and removed stale docs claims that implied dict-only or four-symbol-only API.
- Fixed RST formatting in `docs/contributing.rst` introduced during doc refresh and rebuilt docs.

## Patch summary
- Added metadata regression tests for runtime version parity and CSV helper exports.
- Updated package runtime version to match `pyproject.toml` (`2.0.1`).
- Refreshed README and docs pages (`api`, `configuration`, `contributing`, `examples`, `index`, and Sphinx `conf.py`) to reflect current supported data contracts and API surface.

## PR URL
- https://github.com/Jianxun/yaml2plot/pull/28

## Verification
- `./venv/bin/python -m pytest -q tests/unit/test_package_metadata.py` (pass after fix; initially failed as expected before fix)
- `./venv/bin/python -m pytest -q` (pass: `93 passed`)
- `PATH=\"$(pwd)/venv/bin:$PATH\" make docs` (pass; `build succeeded`)

## Status request (Done / Blocked / In Progress)
- Done

## Blockers / Questions
- None.

## Next steps
- Open PR, set task state to `ready_for_review` with PR number, and hand off to reviewer.
