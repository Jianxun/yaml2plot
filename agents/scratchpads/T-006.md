# T-006 Scratchpad

## Task summary (DoD + verify)
- Title: Introduce unified plotting data adapter
- DoD:
  - Create a single normalization path for plotting inputs used by both Python API and CLI.
  - Eliminate duplicated xarray->dict conversion logic in CLI and plotting layers.
  - Preserve current behavior for dict/xarray/raw path inputs.
- Verify:
  - `pytest tests/unit/plotting -v`
  - `pytest tests/unit/cli -v`

## Read (paths)
- `agents/context/lessons.md`
- `agents/context/contract.md`
- `agents/context/tasks.yaml`
- `agents/context/tasks_state.yaml`
- `agents/context/project_status.md`
- `agents/roles/executor.md`

## Plan
- Add/centralize plotting input normalization in core plotting path.
- Route CLI plotting flow through the same adapter.
- Add tests covering CLI + API parity across dict/xarray/path forms.
- Run task verification commands and capture outputs.

## Milestone notes
- Intake complete.
- Added failing tests for shared normalization usage in CLI + plotting.
- Implemented shared normalization adapter and routed CLI conversion through it.
- Verified plotting and CLI unit suites pass.

## Patch summary
- Added `normalize_plot_data()` in `src/yaml2plot/core/plotting.py` as the single adapter for dict/path/xarray/pandas inputs.
- Refactored `plot()` to call `normalize_plot_data()` instead of inlined conversion logic.
- Updated CLI plot command to call `normalize_plot_data(dataset)` and call `create_plot(..., show=False)` so display remains controlled by CLI flow.
- Added adapter coverage tests:
  - `tests/unit/plotting/test_plotting_helpers.py::TestNormalizePlotData::test_normalize_plot_data_accepts_xarray_dataset`
  - `tests/unit/cli/test_cli_raw_field.py::test_yaml_raw_field_usage` now asserts CLI uses `normalize_plot_data`.

## PR URL
- https://github.com/Jianxun/yaml2plot/pull/26

## Verification
- `./venv/bin/python -m pytest tests/unit/plotting -v` (pass)
- `./venv/bin/python -m pytest tests/unit/cli -v` (pass)

## Status request (Done / Blocked / In Progress)
- Done

## Blockers / Questions
- None.

## Next steps
- Reviewer validation and merge decision.
